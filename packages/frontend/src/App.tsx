import React from "react";

// App.tsx — stub. React Router routes are wired in task 12.2.
function App(): React.ReactElement {
  return (
    <div
      style={{
        display: "flex",
        alignItems: "center",
        justifyContent: "center",
        height: "100vh",
        color: "var(--text-secondary)",
        fontFamily: "inherit",
      }}
    >
      <p>AI Music Generator — loading…</p>
    </div>
  );
}

export default App;
